[Sitemap](https://medium.com/sitemap/sitemap.xml)

[Open in app](https://play.google.com/store/apps/details?id=com.medium.reader&referrer=utm_source%3DmobileNavBar&source=---top_nav_layout_nav-----------------------------------------)

Sign up

[Sign in](https://medium.com/m/signin?operation=login&redirect=https%3A%2F%2Fmedium.com%2Fgoogle-cloud%2Fgoogle-adk-2-collaborative-multi-agent-patterns-a-practical-guide-5c696ba556d2&source=post_page---top_nav_layout_nav-----------------------global_nav------------------)

[Medium Logo](https://medium.com/?source=---top_nav_layout_nav-----------------------------------------)

Get app

[Write](https://medium.com/m/signin?operation=register&redirect=https%3A%2F%2Fmedium.com%2Fnew-story&source=---top_nav_layout_nav-----------------------new_post_topnav------------------)

[Search](https://medium.com/search?source=---top_nav_layout_nav-----------------------------------------)

Sign up

[Sign in](https://medium.com/m/signin?operation=login&redirect=https%3A%2F%2Fmedium.com%2Fgoogle-cloud%2Fgoogle-adk-2-collaborative-multi-agent-patterns-a-practical-guide-5c696ba556d2&source=post_page---top_nav_layout_nav-----------------------global_nav------------------)

![Unknown user](https://miro.medium.com/v2/resize:fill:32:32/1*dmbNkD5D-u45r44go_cf0g.png)

[**Google Cloud - Community**](https://medium.com/google-cloud?source=post_page---publication_nav-e52cf94d98af-5c696ba556d2---------------------------------------)

·

Follow publication

[![Google Cloud - Community](https://miro.medium.com/v2/resize:fill:38:38/1*FUjLiCANvATKeaJEeg20Rw.png)](https://medium.com/google-cloud?source=post_page---post_publication_sidebar-e52cf94d98af-5c696ba556d2---------------------------------------)

A collection of technical articles and blogs published or curated by Google Cloud Developer Advocates. The views expressed are those of the authors and don't necessarily reflect those of Google.

Follow publication

Agent Development Kit

Adk 2

Adk

Agents

Google

# Google ADK 2: Collaborative Multi-Agent Patterns — A Practical Guide

[![Romin Irani](https://miro.medium.com/v2/resize:fill:32:32/1*-JQl2Mvox0IH75DTOgj6Ow.png)](https://medium.com/@iromin?source=post_page---byline--5c696ba556d2---------------------------------------)

[Romin Irani](https://medium.com/@iromin?source=post_page---byline--5c696ba556d2---------------------------------------)

Follow

19 min read

·

3 days ago

105

[Listen](https://medium.com/m/signin?actionUrl=https%3A%2F%2Fmedium.com%2Fplans%3Fdimension%3Dpost_audio_button%26postId%3D5c696ba556d2&operation=register&redirect=https%3A%2F%2Fmedium.com%2Fgoogle-cloud%2Fgoogle-adk-2-collaborative-multi-agent-patterns-a-practical-guide-5c696ba556d2&source=---header_actions--5c696ba556d2---------------------post_audio_button------------------)

Share

In the [Graph Workflows Guide](https://medium.com/google-cloud/google-adk-2-graph-workflows-a-complete-guide-with-code-examples-7e27046b85d5?postPublishedType=initial), we built customer support architectures where you explicitly designed every path : sequential chains, parallel joins, and conditional routing edges. That approach is unbeatable when your business processes are strictly predetermined and deterministic.

However, enterprise customer support and conversational applications frequently encounter open-ended scenarios **where the user’s inquiry dictates which specialist gets invoked**. You have a team of domain experts (billing specialists, technical engineers, shipping lookups, callback schedulers), and the LLM must evaluate intent in real time to coordinate the conversation.

This guide teaches you how to design **Collaborative Multi-Agent Systems** in Google ADK 2. We cover **coordinator dispatching**, **sub-agent transfer modes**, **supervisor-specialist peer review**, and **custom programmatic orchestration**, all tested and validated against live models.

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*2DKyM2SxsHY6PGIO50np1A.png)

Generated by Gemini Notebook

**⚠️ Deprecation Notice: Sequential, Parallel, and Loop Agents**

In ADK 2.6+, `SequentialAgent`, `ParallelAgent`, and `LoopAgent` are **formally deprecated** in favor of the graph-based `Workflow` engine.

If you need fixed step-by-step chains, concurrent fan-out/join, or deterministic loop gates, please refer directly to the [Graph Workflows Guide](https://medium.com/google-cloud/google-adk-2-graph-workflows-a-complete-guide-with-code-examples-7e27046b85d5?postPublishedType=initial). This collaborative guide focuses exclusively on modern multi-agent coordination using `Agent`, sub-agent transfer modes, and custom `BaseAgent`.

## Graphs vs. Collaborative Multi-Agent Teams

Before writing agent code, it is critical to understand the architectural trade-offs between deterministic graph pipelines and collaborative agent teams:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*Pygwq1dDg5JIaOJlo1AQxA.png)

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*6Xf60o3kiCN2MK1rasyHCA.png)

## Environment & Repository Setup

All examples in this guide are located in the companion GitHub repository: [**github.com/rominirani/adk-workflow-patterns**](https://github.com/rominirani/adk-workflow-patterns).

```
# 1. Clone the repository
git clone https://github.com/rominirani/adk-workflow-patterns.git
cd adk-workflow-patterns

# 2. Set up virtual environment
python3 -m venv .venv
source .venv/bin/activate

# 3. Install ADK and Pydantic
pip install google-adk pydantic

# 4. Configure credentials (Vertex AI or Gemini API)
export GOOGLE_CLOUD_PROJECT="your-project-id"
export GOOGLE_CLOUD_LOCATION="us-central1"
export GOOGLE_GENAI_USE_VERTEXAI="True"
# Or using Gemini API Key:
# export GOOGLE_API_KEY="your-gemini-api-key"
```

## Pattern 1 — The Coordinator-Dispatcher

### The Concept & The Problem It Solves

When building customer-facing systems, you often have multiple domain specialists — billing, technical support, shipping, and general policies. In a Graph workflow, routing requires hardcoding keyword rules or training a classifier node that emits explicit `EventActions(route=...)`.

The **Coordinator-Dispatcher (Supervisor)** pattern eliminates rigid routing logic. You provide a list of specialist sub-agents to a top-level Coordinator `Agent`. Each specialist has a clear `description` field defining its area of expertise. When a customer sends a message, the coordinator LLM inspects the available specialists, interprets the customer's intent, and automatically delegates the conversation.

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*mAk5f6N4ZihnBdPEK9ADww.png)

📄 [collaborative-workflows/examples/01\_coordinator\_routing.py](https://github.com/rominirani/adk-workflow-patterns/blob/main/collaborative-workflows/examples/01_coordinator_routing.py) — Run: `python collaborative-workflows/examples/01_coordinator_routing.py`

```
"""01: Coordinator-Dispatcher — LLM routes to the right specialist via Agent descriptions"""
import asyncio
import os
import warnings
import logging

warnings.filterwarnings("ignore", category=UserWarning)
warnings.filterwarnings("ignore", category=ResourceWarning)
logging.getLogger("google.genai").setLevel(logging.ERROR)
logging.getLogger("aiohttp").setLevel(logging.ERROR)

from google.adk.agents import Agent
from google.adk import Runner
from google.adk.sessions import InMemorySessionService
from google.genai import types

MODEL = os.environ.get("ADK_MODEL", "gemini-2.5-flash")

# Specialist agents with clear semantic descriptions
billing_specialist = Agent(
    name="billing_specialist",
    model=MODEL,
    description="Handles billing inquiries: refunds, double charges, payment disputes, invoices, and pricing.",
    instruction="""You are a billing support specialist. Help the customer with their billing issue.
    - Be empathetic and professional
    - If a refund is needed, explain the process (5-7 business days)
    - Always confirm the specific charge or invoice in question
    - Keep your response under 60 words""",
)

tech_specialist = Agent(
    name="tech_specialist",
    model=MODEL,
    description="Handles technical issues: bugs, errors, app crashes, performance problems, and setup help.",
    instruction="""You are a technical support engineer. Help the customer with their technical issue.
    - Ask for error messages or reproduction steps if relevant
    - Provide concise step-by-step troubleshooting
    - Keep your response under 60 words""",
)

shipping_specialist = Agent(
    name="shipping_specialist",
    model=MODEL,
    description="Handles shipping and delivery: tracking, lost packages, delivery estimates, and address changes.",
    instruction="""You are a shipping support specialist. Help with delivery inquiries.
    - Provide tracking information and delivery estimates
    - For lost packages, initiate a trace (48 hours)
    - Keep your response under 60 words""",
)

general_specialist = Agent(
    name="general_specialist",
    model=MODEL,
    description="Handles general questions: company info, store policies, feature requests, and feedback.",
    instruction="""You are a general support agent. Handle miscellaneous customer questions.
    - Be helpful and concise
    - Keep your response under 60 words""",
)

# The Coordinator — routes based on sub_agents descriptions without hardcoded graph edges
coordinator = Agent(
    name="support_concierge",
    model=MODEL,
    instruction="""You are the customer support concierge. Your job is to:
    1. Understand the customer's issue
    2. Route them to the most appropriate specialist based on their domain

    Do NOT attempt to resolve billing, technical, shipping, or general issues yourself.
    ALWAYS delegate to the corresponding specialist.""",
    sub_agents=[billing_specialist, tech_specialist, shipping_specialist, general_specialist],
)

async def main():
    runner = Runner(
        agent=coordinator,
        app_name="support_coordinator",
        session_service=InMemorySessionService(),
    )

    queries = [\
        "I was charged twice for order #12345",\
        "The app crashes every time I try to upload a photo",\
        "Where is my package? It's been 2 weeks!",\
        "What's your refund policy?",\
    ]

    for query in queries:
        session = await runner.session_service.create_session(
            app_name="support_coordinator", user_id="user_1"
        )
        print(f"\n{'='*60}")
        print(f"CUSTOMER: {query}")
        print('='*60)

        content = types.Content(role="user", parts=[types.Part(text=query)])
        async for event in runner.run_async(
            user_id="user_1",
            session_id=session.id,
            new_message=content,
        ):
            if event.content and event.content.parts:
                for part in event.content.parts:
                    if part.text:
                        print(f"[{event.author}]: {part.text}")

    await asyncio.sleep(0.25)

if __name__ == "__main__":
    asyncio.run(main())
```

### Code Walkthrough

- `Agent(description=...)`: Every specialist is given a precise description. The coordinator does not read the full prompt of each specialist; it reads these concise description tags to determine suitability.
- `sub_agents=[...]`: By passing the specialists to the coordinator's `sub_agents` parameter, ADK equips the coordinator with agent-to-agent delegation capabilities under the hood.
- **No Hardcoded Router Node**: Unlike graph workflows where you must return `EventActions(route=...)`, the LLM performs the routing decision organically.
- **Isolated Sessions per Query**: In `main()`, we create a fresh session for each query to showcase clean, unpolluted routing decisions.

### Expected Output

```
Verified Execution Output

============================================================
CUSTOMER: I was charged twice for order #12345
============================================================
[billing_specialist]: I understand your frustration with the double charge for order #12345. I apologize for the inconvenience. We'll investigate this immediately. If a refund is needed, please allow 5-7 business days for processing.

============================================================
CUSTOMER: The app crashes every time I try to upload a photo
============================================================
[tech_specialist]: I understand the app crashes when you upload a photo. Please provide the exact error message and steps to reproduce the crash.

============================================================
CUSTOMER: Where is my package? It's been 2 weeks!
============================================================
[shipping_specialist]: Please provide your tracking number so I can check the status of your package. If it's been 2 weeks, it might be lost, and I can initiate a trace for you.

============================================================
CUSTOMER: What's your refund policy?
============================================================
[general_specialist]: Our refund policy allows returns within 30 days of purchase for a full refund, provided the item is unused and in its original packaging.
```

## Pattern 2 — Sub-Agent Transfer Modes & Lifecycles

### The Concept & The Problem It Solves

In older multi-agent frameworks, delegating to a sub-agent caused the notorious **“stranded conversation”** problem: once control was handed over to a specialist, the specialist would continue chatting with the user indefinitely, often forgetting to yield back to the main assistant.

ADK 2 introduces **Transfer Modes** via the `mode` property on `Agent`, giving developers deterministic lifecycle contracts:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*qQye1NWuAwjr8TNynIr8gg.png)

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*-AzR5gnbBMjzOeuqr7xRjQ.png)

📄 [collaborative-workflows/examples/02\_transfer\_modes.py](https://github.com/rominirani/adk-workflow-patterns/blob/main/collaborative-workflows/examples/02_transfer_modes.py) — Run: `python collaborative-workflows/examples/02_transfer_modes.py`

```
"""02: Sub-Agent Transfer Modes & Lifecycles — single_turn, task, chat"""
import asyncio
import os
import warnings
import logging
from pydantic import BaseModel, Field

warnings.filterwarnings("ignore", category=UserWarning)
warnings.filterwarnings("ignore", category=ResourceWarning)
logging.getLogger("google.genai").setLevel(logging.ERROR)
logging.getLogger("aiohttp").setLevel(logging.ERROR)

from google.adk.agents import Agent
from google.adk import Runner
from google.adk.sessions import InMemorySessionService
from google.genai import types

MODEL = os.environ.get("ADK_MODEL", "gemini-2.5-flash")

class BookingConfirmation(BaseModel):
    date_time: str = Field(description="The agreed date and time")
    contact_number: str = Field(description="Customer contact phone number")
    status: str = Field(default="confirmed", description="Booking status")

# 1. single_turn mode: runs once as a tool-like agent, returns result to coordinator with no user interaction
lookup_agent = Agent(
    name="account_lookup",
    model=MODEL,
    description="Looks up account information. Use when the user asks about their account status or details.",
    instruction="Simulate looking up the user's account. Return: 'Account #42: Active, Gold tier, member since 2022.' Do not ask any questions.",
    mode="single_turn",
)

# 2. task mode: can ask clarifying questions across turns, auto-returns to coordinator once finish_task is called
booking_agent = Agent(
    name="appointment_booker",
    model=MODEL,
    description="Books support callback appointments. Use when the user wants to schedule a callback.",
    instruction="""You are an appointment booking specialist.
    To book a callback, you MUST obtain both:
    1. Preferred date and time
    2. Phone number

    If either is missing, ask the user a polite clarifying question.
    Once you have BOTH pieces of information, confirm the appointment details and call your finish_task tool.""",
    mode="task",
    output_schema=BookingConfirmation,
)

# 3. chat mode (default): full conversational copilot handoff
advisor_agent = Agent(
    name="product_advisor",
    model=MODEL,
    description="Provides detailed product advice and recommendations. Use for product questions.",
    instruction="""You are a product advisor. Have a detailed conversation about the user's needs.
    When the conversation is complete, transfer back to the coordinator.""",
    mode="chat",
)

# The Coordinator
coordinator = Agent(
    name="coordinator",
    model=MODEL,
    instruction="""You are the support coordinator. Based on the user's request:
    - For account inquiries → transfer to account_lookup
    - For scheduling → transfer to appointment_booker
    - For product advice → transfer to product_advisor

    Always delegate. When a specialist finishes a task, summarize the confirmation to the customer.""",
    sub_agents=[lookup_agent, booking_agent, advisor_agent],
)

async def main():
    runner = Runner(
        agent=coordinator,
        app_name="transfer_modes",
        session_service=InMemorySessionService(),
    )

    # TEST 1: single_turn mode (Account Lookup)
    print("=== TEST 1: single_turn mode (Account Lookup) ===")
    print("User: 'What is my account status?'\n")
    session1 = await runner.session_service.create_session(
        app_name="transfer_modes", user_id="user_1"
    )
    content1 = types.Content(role="user", parts=[types.Part(text="What is my account status?")])
    async for event in runner.run_async(
        user_id="user_1", session_id=session1.id, new_message=content1,
    ):
        if event.content and event.content.parts:
            for part in event.content.parts:
                if part.text:
                    print(f"[{event.author}]: {part.text}")

    # TEST 2: task mode (Multi-Turn Callback Booking)
    print("\n" + "="*60)
    print("=== TEST 2: task mode (Multi-Turn Callback Booking) ===")
    print("="*60)

    session2 = await runner.session_service.create_session(
        app_name="transfer_modes", user_id="user_2"
    )

    # Turn 1: User initiates booking without required parameters
    turn1_msg = "I need to schedule a technical support callback."
    print(f"\n[User -> Turn 1]: {turn1_msg}")
    content_t1 = types.Content(role="user", parts=[types.Part(text=turn1_msg)])
    async for event in runner.run_async(
        user_id="user_2", session_id=session2.id, new_message=content_t1,
    ):
        if event.content and event.content.parts:
            for part in event.content.parts:
                if part.text:
                    print(f"[{event.author}]: {part.text}")

    # Turn 2: User provides missing phone and datetime in the same session
    turn2_msg = "Tomorrow at 2:00 PM EST. You can reach me at 555-0199."
    print(f"\n[User -> Turn 2]: {turn2_msg}")
    content_t2 = types.Content(role="user", parts=[types.Part(text=turn2_msg)])
    async for event in runner.run_async(
        user_id="user_2", session_id=session2.id, new_message=content_t2,
    ):
        if event.content and event.content.parts:
            for part in event.content.parts:
                if part.text:
                    print(f"[{event.author}]: {part.text}")

    await asyncio.sleep(0.25)

if __name__ == "__main__":
    asyncio.run(main())
```

### Code Walkthrough

- `mode="single_turn"`: `account_lookup` runs once and immediately yields back. Notice how the coordinator receives the result and provides the final customer response.
- `mode="task"` **\+**`output_schema`: `appointment_booker` is bound to the `BookingConfirmation` Pydantic model. ADK automatically provisions a `finish_task` tool matching this schema.
- **Multi-turn Clarification**: In Turn 1, because the user did not supply a phone number or time, the agent asked a clarifying question. In Turn 2, once the fields were supplied, it completed the task and returned control to the coordinator.

### Expected Output

```
=== TEST 1: single_turn mode (Account Lookup) ===
User: 'What is my account status?'

[account_lookup]: Account #42: Active, Gold tier, member since 2022.
[coordinator]: Account #42 is active, Gold tier, and has been a member since 2022.

============================================================
=== TEST 2: task mode (Multi-Turn Callback Booking) ===
============================================================

[User -> Turn 1]: I need to schedule a technical support callback.
[appointment_booker]: I can help you schedule a technical support callback. What is your preferred date and time for the callback, and what is your phone number?

[User -> Turn 2]: Tomorrow at 2:00 PM EST. You can reach me at 555-0199.
[coordinator]: Your technical support callback has been scheduled for Tomorrow at 2:00 PM EST. A specialist will call you at 555-0199.
```

## Pattern 3 — Multi-Agent Collaboration (Supervisor, Drafter & Critic)

### The Concept & The Problem It Solves

In high-stakes technical support or legal environments, generating a direct one-shot response from a single prompt often introduces hallucinations, overly terse instructions, or safety risks.

## Get Romin Irani’s stories in your inbox

Join Medium for free to get updates from this writer.

Subscribe

Subscribe

Remember me for faster sign in

Instead of using deprecated loop constructs (like `LoopAgent`), this pattern demonstrates **Multi-Agent Peer Review**: an Editorial Supervisor coordinates between a specialized **Technical Drafter** (focused purely on deep technical diagnosis) and a **Quality Critic** (focused on customer empathy, safety, and polish).

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*a4HfGDRf5kvRaa6GZUikQw.png)

📄 [collaborative-workflows/examples/03\_supervisor\_collaboration.py](https://github.com/rominirani/adk-workflow-patterns/blob/main/collaborative-workflows/examples/03_supervisor_collaboration.py) — Run: `python collaborative-workflows/examples/03_supervisor_collaboration.py`

```
"""03: Multi-Agent Collaboration — Supervisor coordinating Drafter & Critic specialists"""
import asyncio
import os
import warnings
import logging

warnings.filterwarnings("ignore", category=UserWarning)
warnings.filterwarnings("ignore", category=ResourceWarning)
logging.getLogger("google.genai").setLevel(logging.ERROR)
logging.getLogger("aiohttp").setLevel(logging.ERROR)

from google.adk.agents import Agent
from google.adk import Runner
from google.adk.sessions import InMemorySessionService
from google.genai import types

MODEL = os.environ.get("ADK_MODEL", "gemini-2.5-flash")

# Specialist 1: Technical Drafter (Single Turn tool-like specialist)
tech_drafter = Agent(
    name="tech_drafter",
    model=MODEL,
    description="Drafts initial technical troubleshooting steps and resolutions for complex software issues.",
    instruction="""You are a technical troubleshooting drafter.
    Analyze the user's issue and write a concise, 3-step action plan to resolve it.
    Be precise, technical, and concise (under 60 words).""",
    mode="single_turn",
)

# Specialist 2: Quality & Safety Critic (Single Turn review specialist)
quality_critic = Agent(
    name="quality_critic",
    model=MODEL,
    description="Reviews technical drafts for safety, customer empathy, clarity, and policy compliance.",
    instruction="""You are a senior support quality reviewer.
    Review the drafted response in the conversation.
    - Ensure it is empathetic and free of risky instructions
    - Provide a polished, customer-ready version of the response
    - Keep it under 60 words and sign off as 'Expert Support Team'""",
    mode="single_turn",
)

# The Supervisor Agent orchestrating the collaboration between Drafter and Critic
editorial_supervisor = Agent(
    name="editorial_supervisor",
    model=MODEL,
    instruction="""You are the technical support lead orchestrating high-quality responses.
    For technical customer inquiries:
    1. First, delegate to `tech_drafter` to generate a technical diagnosis and resolution plan.
    2. Next, delegate the draft to `quality_critic` to polish and review it for the customer.
    3. Finally, deliver the approved, polished response to the customer.

    Ensure both specialists contribute before finalizing the answer.""",
    sub_agents=[tech_drafter, quality_critic],
)

async def main():
    runner = Runner(
        agent=editorial_supervisor,
        app_name="supervisor_collaboration",
        session_service=InMemorySessionService(),
    )

    tickets = [\
        "My database connection pool keeps exhausting during peak traffic and throwing 500 errors on our checkout page.",\
        "Users report that session cookies are being invalidated immediately after login on mobile Safari.",\
    ]

    for ticket in tickets:
        session = await runner.session_service.create_session(
            app_name="supervisor_collaboration", user_id="user_1"
        )
        print(f"\n{'='*60}")
        print(f"CUSTOMER TICKET: {ticket}")
        print('='*60)

        content = types.Content(role="user", parts=[types.Part(text=ticket)])
        async for event in runner.run_async(
            user_id="user_1",
            session_id=session.id,
            new_message=content,
        ):
            if event.content and event.content.parts:
                for part in event.content.parts:
                    if part.text:
                        print(f"[{event.author}]:\n{part.text}\n")

    await asyncio.sleep(0.25)

if __name__ == "__main__":
    asyncio.run(main())
```

### Code Walkthrough

- `tech_drafter` **(Specialist 1)**: Operates in `single_turn` mode. Its sole responsibility is to analyze the technical error and generate a 3-step action plan.
- `quality_critic` **(Specialist 2)**: Also in `single_turn` mode. It inspects the technical draft within the shared conversation context and rewrites it with professional polish.
- `editorial_supervisor` **(Coordinator)**: Instructed to invoke both agents sequentially before synthesizing and sending the final answer to the customer.

### Expected Output

```
============================================================
CUSTOMER TICKET: My database connection pool keeps exhausting during peak traffic and throwing 500 errors on our checkout page.
============================================================
[tech_drafter]:
1. **Review & Adjust Pool Size:** Increase database connection pool size and database's max connections to handle peak load.
2. **Optimize Queries:** Profile and optimize slow database queries and add missing indexes to reduce connection holding time.
3. **Implement Connection Health Checks:** Configure connection validators and gracefully handle stale connections or implement retry logic.

[quality_critic]:
We appreciate you sharing these valuable insights. Optimizing database connection pools, queries, and implementing connection health checks are indeed key best practices for ensuring system stability and performance. Thank you for these thoughtful recommendations.

Expert Support Team

[editorial_supervisor]:
My apologies for the issues you're experiencing with your checkout page during peak traffic. Database connection pool exhaustion can definitely lead to 500 errors, and we understand the urgency of resolving this.

Here’s a plan to address the problem:
1.  **Review & Adjust Pool Size:** We will examine your current database connection pool settings and increase both the pool size and the database's maximum allowed connections.
2.  **Optimize Queries:** Our team will profile your database queries to identify any slow-running operations. We'll then optimize these queries and add necessary indexes.
3.  **Implement Connection Health Checks:** We'll configure connection validators and implement retry logic.

Expert Support Team
```

## Pattern 4 — Custom Orchestration via \`BaseAgent\`

### The Concept & The Problem It Solves

While LLM-based coordinator dispatching is versatile, enterprise architectures often require **programmatic determinism**: inspecting session state variables (such as ticket priority, customer VIP status, or authentication tokens), applying circuit breakers, or logging custom metrics.

By subclassing `BaseAgent` and implementing the `_run_async_impl(self, ctx: Context)` generator, you gain full control over how and when sub-agents execute.

📄 [collaborative-workflows/examples/04\_custom\_orchestrator.py](https://github.com/rominirani/adk-workflow-patterns/blob/main/collaborative-workflows/examples/04_custom_orchestrator.py) — Run: `python collaborative-workflows/examples/04_custom_orchestrator.py`

```
"""04: Custom Multi-Agent Orchestrator — BaseAgent with programmatic control & session state"""
import asyncio
import os
import warnings
import logging

warnings.filterwarnings("ignore", category=UserWarning)
warnings.filterwarnings("ignore", category=ResourceWarning)
logging.getLogger("google.genai").setLevel(logging.ERROR)
logging.getLogger("aiohttp").setLevel(logging.ERROR)

from google.adk.agents import BaseAgent, Agent
from google.adk.agents.context import Context
from google.adk import Runner
from google.adk.sessions import InMemorySessionService
from google.genai import types

MODEL = os.environ.get("ADK_MODEL", "gemini-2.5-flash")

# Specialized agents
triage_agent = Agent(
    name="triage_agent",
    model=MODEL,
    instruction="""Classify the ticket priority. Reply with ONLY one of: P1, P2, P3, or P4.
    P1 = entire system down or data breach.
    P2 = major broken feature affecting revenue.
    P3 = general software bug.
    P4 = question or account help.""",
    output_key="triage_priority",
)

enrichment_agent = Agent(
    name="enrichment_agent",
    model=MODEL,
    instruction="""Simulate fetching VIP account status. Output:
    'Customer Tier: Platinum Enterprise | SLA: 15-minute response | Account Value: $250k/yr'""",
    output_key="customer_enrichment",
    mode="single_turn",
)

urgent_responder = Agent(
    name="urgent_responder",
    model=MODEL,
    instruction="""You are an executive incident responder. Draft an urgent, high-priority SLA response.
    Acknowledge the enterprise tier and commit to an immediate war room ETA within 15 minutes.
    Keep it under 60 words. Sign off as 'Executive Escalation Team'.""",
)

standard_responder = Agent(
    name="standard_responder",
    model=MODEL,
    instruction="""You are a helpful customer support representative.
    Provide a friendly, helpful answer to the customer's question in under 50 words.
    Sign off as 'Customer Support Team'.""",
)

class SmartCustomOrchestrator(BaseAgent):
    """Custom BaseAgent that inspects session state and coordinates multi-agent flow."""

    model: str = MODEL

    async def _run_async_impl(self, ctx: Context):
        # Step 1: Run triage agent and stream events
        async for event in triage_agent.run_async(ctx):
            yield event

        # Step 2: Read state from session (ctx.session.state)
        raw_priority = ctx.session.state.get("triage_priority", "P3")
        priority = raw_priority.strip().upper() if isinstance(raw_priority, str) else "P3"
        print(f"\n[CustomOrchestrator] Evaluated Priority in state: {priority}")

        # Step 3: Programmatic dynamic routing & multi-agent execution
        if any(p in priority for p in ("P1", "P2")):
            print("[CustomOrchestrator] Escalating: Executing VIP Enrichment + Urgent Responder")
            async for event in enrichment_agent.run_async(ctx):
                yield event
            async for event in urgent_responder.run_async(ctx):
                yield event
        else:
            print("[CustomOrchestrator] Standard Flow: Executing Standard Responder")
            async for event in standard_responder.run_async(ctx):
                yield event

smart_orchestrator = SmartCustomOrchestrator(name="smart_orchestrator")

async def main():
    runner = Runner(
        agent=smart_orchestrator,
        app_name="custom_orchestrator",
        session_service=InMemorySessionService(),
    )

    tickets = [\
        ("Our production database is unreachable and all API requests are failing 100%!", "P1 Critical Ticket"),\
        ("Where can I find the invoice history for last quarter?", "P4 Standard Ticket"),\
    ]

    for ticket, label in tickets:
        session = await runner.session_service.create_session(
            app_name="custom_orchestrator", user_id="user_1"
        )
        print(f"\n{'='*60}")
        print(f"[{label}] {ticket}")
        print('='*60)

        content = types.Content(role="user", parts=[types.Part(text=ticket)])
        async for event in runner.run_async(
            user_id="user_1",
            session_id=session.id,
            new_message=content,
        ):
            if event.content and event.content.parts:
                for part in event.content.parts:
                    if part.text:
                        print(f"[{event.author}]: {part.text}")

    await asyncio.sleep(0.25)

if __name__ == "__main__":
    asyncio.run(main())
```

### Code Walkthrough

- `output_key="triage_priority"`: Tells ADK to save the text generated by `triage_agent` directly into `ctx.session.state["triage_priority"]`.
- `ctx.session.state.get(...)`: In `_run_async_impl`, you access the shared session whiteboard via `ctx.session.state`.
- **Programmatic Branching**: If the state evaluates to `P1` or `P2`, the orchestrator invokes the VIP enrichment agent followed by the executive responder; otherwise it invokes the standard responder.
- `async for event in agent.run_async(ctx): yield event`: Propagates all emitted event streams up to the runner transparently.

### Expected Output

```
============================================================
[P1 Critical Ticket] Our production database is unreachable and all API requests are failing 100%!
============================================================
[triage_agent]: P1

[CustomOrchestrator] Evaluated Priority in state: P1
[CustomOrchestrator] Escalating: Executing VIP Enrichment + Urgent Responder
[enrichment_agent]: Customer Tier: Platinum Enterprise | SLA: 15-minute response | Account Value: $250k/yr
[urgent_responder]: Acknowledged. Your Platinum Enterprise tier P1 incident is our highest priority. We are immediately initiating a war room. Executive leadership is engaged, and a dedicated team will be assembled with an ETA of within 15 minutes. Updates will follow.

Executive Escalation Team

============================================================
[P4 Standard Ticket] Where can I find the invoice history for last quarter?
============================================================
[triage_agent]: P4

[CustomOrchestrator] Evaluated Priority in state: P4
[CustomOrchestrator] Standard Flow: Executing Standard Responder
[standard_responder]: You can easily find your invoice history for last quarter by logging into your account portal. Navigate to the 'Billing' or 'Invoice History' section, where you can view and download all past invoices.

Customer Support Team
```

## Pattern 5 — The Complete Enterprise Support Concierge

### The Concept & The Problem It Solves

In an enterprise application, you don’t use just one pattern in isolation. A realistic customer support system combines:

- A central **Coordinator** managing incoming traffic.
- A **Single-Turn** agent for silent background tracking lookups.
- A **Multi-Turn Task Mode** agent with a validated Pydantic schema for callback scheduling.
- Dedicated **Chat Mode** agents for in-depth billing and technical resolution.

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*kVCOOmkfv7_w2Ibv91figg.png)

📄 [collaborative-workflows/examples/05\_complete\_support\_system.py](https://github.com/rominirani/adk-workflow-patterns/blob/main/collaborative-workflows/examples/05_complete_support_system.py) — Run: `python collaborative-workflows/examples/05_complete_support_system.py`

```
"""05: Complete Collaborative Support System — Bringing all multi-agent patterns together"""
import asyncio
import os
import warnings
import logging
from pydantic import BaseModel, Field

warnings.filterwarnings("ignore", category=UserWarning)
warnings.filterwarnings("ignore", category=ResourceWarning)
logging.getLogger("google.genai").setLevel(logging.ERROR)
logging.getLogger("aiohttp").setLevel(logging.ERROR)

from google.adk.agents import Agent
from google.adk import Runner
from google.adk.sessions import InMemorySessionService
from google.genai import types

MODEL = os.environ.get("ADK_MODEL", "gemini-2.5-flash")

# --- Schema for Structured Task Mode ---
class CallbackRequest(BaseModel):
    contact_phone: str = Field(description="Customer phone number")
    preferred_time: str = Field(description="Preferred callback window e.g. 2pm Tomorrow")
    issue_topic: str = Field(description="Topic of callback")

# --- 1. Billing Specialist (Chat Mode) ---
billing_specialist = Agent(
    name="billing_specialist",
    model=MODEL,
    description="Handles billing inquiries: refunds, incorrect charges, subscription upgrades, and invoices.",
    instruction="""You are a billing support expert.
    - Be empathetic, courteous, and professional
    - Explain refund timelines (5-7 business days)
    - Keep responses under 50 words and sign off as 'Billing Specialist'""",
    mode="chat",
)

# --- 2. Shipping Lookup Specialist (Single-Turn Mode) ---
shipping_lookup = Agent(
    name="shipping_lookup",
    model=MODEL,
    description="Performs quick shipping tracking status lookups for order numbers and lost package inquiries.",
    instruction="""Simulate looking up tracking for the requested order.
    Return: 'Order #789: In Transit with FedEx. Estimated Delivery: Tomorrow by 4:00 PM.'
    Keep response under 40 words and do not ask follow-up questions.""",
    mode="single_turn",
)

# --- 3. Technical Troubleshooter Specialist (Chat Mode) ---
tech_specialist = Agent(
    name="tech_specialist",
    model=MODEL,
    description="Diagnoses and troubleshoots technical errors, crashes, API failures, and bugs.",
    instruction="""You are a senior technical support engineer.
    Provide immediate, actionable debugging steps for technical errors.
    Keep response under 50 words and sign off as 'Tech Engineering'.""",
    mode="chat",
)

# --- 4. Appointment & Callback Scheduler (Task Mode) ---
callback_scheduler = Agent(
    name="callback_scheduler",
    model=MODEL,
    description="Schedules technical support phone callbacks. Use when the user requests a phone call or callback.",
    instruction="""You are the callback booking agent.
    Collect both:
    1. Customer phone number
    2. Preferred callback date/time
    Once you have both, confirm the appointment and call your finish_task tool.""",
    mode="task",
    output_schema=CallbackRequest,
)

# --- Central Concierge Coordinator ---
coordinator = Agent(
    name="support_concierge",
    model=MODEL,
    instruction="""You are the customer support concierge for an enterprise platform.
    Your role is to understand the customer's request and delegate to the right specialist:
    - Transfer billing & charge questions to `billing_specialist`
    - Transfer package and shipping status inquiries to `shipping_lookup`
    - Transfer crashes, bugs, and API errors to `tech_specialist`
    - Transfer callback and phone appointment requests to `callback_scheduler`

    Always delegate. When a specialist completes their work, provide a warm summary to the customer.""",
    sub_agents=[billing_specialist, shipping_lookup, tech_specialist, callback_scheduler],
)

async def main():
    runner = Runner(
        agent=coordinator,
        app_name="complete_support_system",
        session_service=InMemorySessionService(),
    )

    test_scenarios = [\
        ("Billing Inquiry", "I noticed a double charge on my subscription for last month."),\
        ("Shipping Lookup", "Where is my package? Order number is #789."),\
        ("Technical Support", "Our mobile app crashes whenever we click the export CSV button."),\
    ]

    for label, query in test_scenarios:
        session = await runner.session_service.create_session(
            app_name="complete_support_system", user_id="user_enterprise"
        )
        print(f"\n{'='*65}")
        print(f"[{label}] CUSTOMER: {query}")
        print('='*65)

        content = types.Content(role="user", parts=[types.Part(text=query)])
        async for event in runner.run_async(
            user_id="user_enterprise",
            session_id=session.id,
            new_message=content,
        ):
            if event.content and event.content.parts:
                for part in event.content.parts:
                    if part.text:
                        print(f"[{event.author}]: {part.text}")

    await asyncio.sleep(0.25)

if __name__ == "__main__":
    asyncio.run(main())
```

### Code Walkthrough

- **Multi-Paradigm Composition**: In a single coordinator configuration, we mix `mode="single_turn"` (instant lookup), `mode="task"` (form completion), and `mode="chat"` (interactive troubleshooting).
- **Zero Pipeline Bloat**: We do not configure complex condition graphs or state schema routers — the LLM coordinator matches customer intents accurately to each specialized agent.
- **Production Cleanliness**: Sub-agents do not pollute other domains; each specialist focuses on its single area of expertise.

```
=================================================================
[Billing Inquiry] CUSTOMER: I noticed a double charge on my subscription for last month.
=================================================================
[billing_specialist]: I understand your concern about the double charge. I've initiated a refund for the extra charge, which should reflect in your account within 5-7 business days. We appreciate your patience.

Billing Specialist

=================================================================
[Shipping Lookup] CUSTOMER: Where is my package? Order number is #789.
=================================================================
[shipping_lookup]: Order #789: In Transit with FedEx. Estimated Delivery: Tomorrow by 4:00 PM.
[support_concierge]: Order #789: In Transit with FedEx. Estimated Delivery: Tomorrow by 4:00 PM.

=================================================================
[Technical Support] CUSTOMER: Our mobile app crashes whenever we click the export CSV button.
=================================================================
[tech_specialist]: Check app logs for crash details. Reproduce on different devices/OS versions. Update app to the latest version. Reinstall if issue persists.
Tech Engineering
```

## Architecture Decision Matrix & Conclusion

When engineering generative AI applications in production, use this 4-step checklist to select your pattern:

1. **“Can I draw the complete flowchart on a whiteboard before the user inputs anything?”**

→ **Yes:** Use **Graph Workflows (**`Workflow` **)**. You save tokens and guarantee deterministic execution.
2. **“Does the user’s message dictate which expert should respond?”**

→ **Yes:** Use a **Coordinator-Dispatcher (**`Agent(sub_agents=[...])` **)**.
3. **“Does the sub-agent need to look up data silently, or collect structured forms across turns?”**

→ **Silent helper:** Use `mode="single_turn"`.

→ **Form / Booking:** Use `mode="task"` with a Pydantic `output_schema`.
4. **“Can I mix both?”**

→ **Yes!** In enterprise systems, top-level Coordinators frequently delegate to deterministic Graph Workflows or Custom Agents for sensitive compliance steps.

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*Qi4acA5UJhakaXUv6x-4rQ.png)

## The Production Sweet Spot — Hybrid Architectures

In enterprise-grade AI systems, the debate is never strictly “Graphs vs. Autonomous Multi-Agent Teams”. The real superpower of ADK 2 is that you can **seamlessly compose both paradigms together into a Hybrid Architecture**:

Press enter or click to view image in full size

![](https://miro.medium.com/v2/resize:fit:700/1*k6JR8QqbLha1CAJApvwP1g.png)

By placing an `Agent` coordinator at the front door to handle human ambiguity and routing, while routing sensitive backend operations (like refunds, database transformations, or fraud verification) to deterministic `Workflow` graphs, you get the best of both worlds:

- **Maximum User Flexibility**: Users can speak naturally, ask questions out of order, and switch topics without breaking rigid state machines.
- **Uncompromising Cost & Security Controls**: Critical multi-step data pipelines run with 0 token overhead and mathematically verified transition edges.
- **Strict Contract Validation**: Sub-agents using `mode="task"` guarantee that data returning to the supervisor adheres strictly to your Pydantic data models.

## Conclusion & some tips for ADK Developers

1. **Descriptions are your API contracts:** In collaborative systems, the LLM reads agent `description` strings to decide who executes. Make them distinct, specific, and mutually exclusive.
2. **Pick the right transfer mode:** Use `mode="single_turn"` for silent lookups, `mode="task"` with typed `output_schema` for structured forms, and `mode="chat"` for conversational copilots. Never leave a sub-agent stranded.
3. **Retire deprecated patterns:** Don’t use `SequentialAgent`, `ParallelAgent`, or `LoopAgent` in new projects. For deterministic chains, parallel execution, and loops, use `Workflow` graphs.
4. **Leverage**`ctx.session.state` **:** Treat session state as the shared whiteboard between your agents. Use `output_key` on agents to persist outputs automatically.

You now have the complete blueprint for orchestrating both deterministic graphs and autonomous multi-agent teams with Google’s Agent Development Kit (ADK 2).

Agent Development Kit

Adk 2

Adk

Agents

Google

[![Google Cloud - Community](https://miro.medium.com/v2/resize:fill:48:48/1*FUjLiCANvATKeaJEeg20Rw.png)](https://medium.com/google-cloud?source=post_page---post_publication_info--5c696ba556d2---------------------------------------)

[![Google Cloud - Community](https://miro.medium.com/v2/resize:fill:64:64/1*FUjLiCANvATKeaJEeg20Rw.png)](https://medium.com/google-cloud?source=post_page---post_publication_info--5c696ba556d2---------------------------------------)

Follow

[**Published in Google Cloud - Community**](https://medium.com/google-cloud?source=post_page---post_publication_info--5c696ba556d2---------------------------------------)

[77K followers](https://medium.com/google-cloud/followers?source=post_page---post_publication_info--5c696ba556d2---------------------------------------)

· [Last published just now](https://medium.com/google-cloud/closing-the-sap-visibility-gap-with-ai-powered-google-secops-7b68649d58f0?source=post_page---post_publication_info--5c696ba556d2---------------------------------------)

A collection of technical articles and blogs published or curated by Google Cloud Developer Advocates. The views expressed are those of the authors and don't necessarily reflect those of Google.

Follow

[![Romin Irani](https://miro.medium.com/v2/resize:fill:48:48/1*-JQl2Mvox0IH75DTOgj6Ow.png)](https://medium.com/@iromin?source=post_page---post_author_info--5c696ba556d2---------------------------------------)

[![Romin Irani](https://miro.medium.com/v2/resize:fill:64:64/1*-JQl2Mvox0IH75DTOgj6Ow.png)](https://medium.com/@iromin?source=post_page---post_author_info--5c696ba556d2---------------------------------------)

Follow

[**Written by Romin Irani**](https://medium.com/@iromin?source=post_page---post_author_info--5c696ba556d2---------------------------------------)

[8.2K followers](https://medium.com/@iromin/followers?source=post_page---post_author_info--5c696ba556d2---------------------------------------)

· [458 following](https://medium.com/@iromin/following?source=post_page---post_author_info--5c696ba556d2---------------------------------------)

Developer Advocate @ Google. My passion is to help developers succeed. ¯\\\_(ツ)\_/¯

Follow

[Help](https://help.medium.com/hc/en-us?source=post_page-----5c696ba556d2---------------------------------------)

[Status](https://status.medium.com/?source=post_page-----5c696ba556d2---------------------------------------)

[About](https://medium.com/about?autoplay=1&source=post_page-----5c696ba556d2---------------------------------------)

[Careers](https://medium.com/jobs-at-medium/work-at-medium-959d1a85284e?source=post_page-----5c696ba556d2---------------------------------------)

[Press](mailto:pressinquiries@medium.com)

[Blog](https://blog.medium.com/?source=post_page-----5c696ba556d2---------------------------------------)

[Store](https://medium.com/store)

[Privacy](https://policy.medium.com/medium-privacy-policy-f03bf92035c9?source=post_page-----5c696ba556d2---------------------------------------)

[Rules](https://policy.medium.com/medium-rules-30e5502c4eb4?source=post_page-----5c696ba556d2---------------------------------------)

[Terms](https://policy.medium.com/medium-terms-of-service-9db0094a1e0f?source=post_page-----5c696ba556d2---------------------------------------)

[Text to speech](https://speechify.com/medium?source=post_page-----5c696ba556d2---------------------------------------)